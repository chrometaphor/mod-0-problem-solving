// <!-- Solve at least 3 of the challenges below. Create a file 
// using the language for your program 
// (`challenge1.rb` or `challenge1.js`) for each of the 
// challenges you select. For each challenge you complete, 
// include comments showing the **overall goal** in your own words, 
// your **pseudocode**, and your **final solution**.  -->

// <!-- 1. Given an array of strings, return only the strings that have exactly 4 characters. -->
// function four() {


    var four = ["laugh", "eat", "play", "cats"]
    for (i = 0; i < four.length; i++) {
        if (four[i].length === 4) {
            console.log(four[i])
        }
    } 

    // make an array of different words in strings that have different lengths
    // using a for statement is what cycles through strings
    // using an if statement



    // 1. Start with an array of strings with a mix of uppercase and lowercase letters. 
    // Print every word in all lowercase letters.

    // var mix = ["mix", "SHIRT", "day", "LIFT"]

